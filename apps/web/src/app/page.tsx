import { LandingHeroWidget } from '@/widgets/landing-hero'

export default function HomePage() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-background via-muted to-accent/40">
      <LandingHeroWidget />
    </main>
  )
}
